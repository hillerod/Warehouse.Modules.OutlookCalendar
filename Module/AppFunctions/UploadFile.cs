
namespace Module.AppFunctions
{
    public static class UploadFile
    {
        //[FunctionName("UploadFile")]
        //public static HttpResponseData Run([HttpTrigger(AuthorizationLevel.Function, "get", "post")] HttpRequestData req,FunctionContext executionContext)
        //{
        //    var log = executionContext.GetLogger("UploadFile");

        //    if (!log.VerifyApiKey(req, out string apiKey))
        //        return req.ErrorResponse(string.IsNullOrEmpty(apiKey) ? "Apikey is missing." : "Apikey is not correct.");






        //    log.LogInformation("File uploaded");
        //    var response = req.CreateResponse(HttpStatusCode.OK);
        //    return response;
        //}
    }
}
